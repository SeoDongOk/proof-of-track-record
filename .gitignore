# Compact 컴파일 산출물 (재생성 가능, 증명키가 커서 커밋하지 않음)
build/
src/managed/

# Node
node_modules/
dist/
*.tsbuildinfo

# 로컬 설정/비밀
.env
.env.local
*.seed
wallet-*.json

# macOS
.DS_Store

# 툴 상태
.bkit/

# 증명 입력 (비공개 witness: salt 포함)
trades*.json

# 배포 결과는 devnet 재기동 시 무효
deployed-*.json

# 실행 중 생기는 산출물
logs/
midnight-level-db/

# archify 시각 검사 부산물 (재생성 가능)
diagrams/*.visual-check.*

# 덱 미리보기 (python deck/preview.py 로 재생성)
deck/preview/
deck/preview-talk/
